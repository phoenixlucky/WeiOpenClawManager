{
  "name": "WeiOpenClawManager",
  "version": "1.7.0",
  "description": "WeiLong OpenClaw config manager desktop client",
  "main": "electron-main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "start:web": "node server.js",
    "build:win": "electron-builder --win nsis -c.electronDist=node_modules/electron/dist",
    "build:installer": "electron-builder --win nsis -c.electronDist=node_modules/electron/dist"
  },
  "devDependencies": {
    "electron": "^41.2.0",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.openclaw.manager",
    "productName": "尉龙虾OpenClaw配置管理",
    "executableName": "WeiOpenClawManager",
    "directories": {
      "output": "dist/electron",
      "buildResources": "build"
    },
    "files": [
      "electron-main.js",
      "electron-preload.js",
      "server.js",
      "public/**/*",
      "build/icon.png",
      "package.json"
    ],
    "extraFiles": [
      {
        "from": "build/icon.ico",
        "to": "icon.ico"
      }
    ],
    "asar": true,
    "win": {
      "signAndEditExecutable": false,
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "build/icon.ico",
      "artifactName": "WeiOpenClawManager-Setup-${version}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": true,
      "allowToChangeInstallationDirectory": true,
      "include": "build/installer.nsh",
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "尉龙虾OpenClaw配置管理",
      "installerIcon": "build/installerIcon.ico",
      "uninstallerIcon": "build/uninstallerIcon.ico",
      "installerHeaderIcon": "build/installerHeaderIcon.ico",
      "installerHeader": "build/installerHeader.bmp",
      "installerSidebar": "build/installerSidebar.bmp",
      "artifactName": "WeiOpenClawManager-Setup-${version}.${ext}"
    }
  },
  "engines": {
    "node": ">=22"
  }
}
